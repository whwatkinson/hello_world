FROM ubuntu:22.04

RUN apt-get update -y && \
    apt-get install tcl -y

COPY hello_world.tcl .

# TODO This works but not in the spirit of the challenge
CMD ["tclsh", "hello_world.tcl"]
RUN tclsh hello_world.tcl >> output.txt
CMD ["cat", "output.txt"]
