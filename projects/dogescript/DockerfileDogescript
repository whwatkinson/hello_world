FROM node:19.4.0

RUN npm install -g npm@latest \
    && npm install dogescript -g

COPY hello_world.djs .

RUN dogescript hello_world.bcl.nu.v.gd.djs > hello_world.bcl.nu.v.gd.js

CMD ["node", "hello_world.js"]
